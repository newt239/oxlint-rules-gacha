# assets

OG 画像（`next/og`）の描画に使うフォントです。

- `Baloo2-Regular.ttf` / `Baloo2-Bold.ttf`: Google Fonts の Baloo 2 を ASCII (U+0020–U+007E) にサブセット化したもの。`ImageResponse` は woff2 を扱えず、フルセットは 1 ファイル 400KB 超でバンドル上限 500KB に収まらないため。
- `OFL.txt`: 上記フォントのライセンス（SIL Open Font License 1.1）。
